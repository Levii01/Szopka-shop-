<div class="layout-row layout-xs-column layout-padding flex">
  <div>
    <h1>{{model.name}}</h1>


    {{#paper-card as |card|}}
      {{card.image src="http://placekitten.com/372/600" alt="Washed Out" title="Washed Out"}}
      {{#card.actions}}
        {{#paper-button iconButton=true}}{{paper-icon "favorite"}}{{/paper-button}}
        {{#paper-button iconButton=true}}{{paper-icon "settings"}}{{/paper-button}}
        {{#paper-button iconButton=true}}{{paper-icon "share"}}{{/paper-button}}
      {{/card.actions}}
    {{/paper-card}}
  </div>

  <div>
    <h2 class="price"> <strong>{{model.price}}$</strong></h2>
<span><br><br></span>
<h1>Description product: </h1>
<br>
    <h2>{{model.description}}</h2>
  </div>
</div>
  {{outlet}}
